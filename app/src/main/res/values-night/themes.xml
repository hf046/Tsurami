<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Tsurami" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/clear_blue</item>
        <item name="colorPrimaryVariant">@color/clear_violet</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/clear_red</item>
        <item name="colorSecondaryVariant">@color/clear_orange</item>
        <item name="colorOnSecondary">@color/clear_crimson</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="recyclerview">
        <item name="android:background">@color/clear_green</item>
    </style>

    <style name="feeling_record">
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:background">@color/clear_chartreuse</item>
        <item name="android:textAppearance">@android:style/TextAppearance.Large</item>
    </style>

    <style name="input_element">
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:background">@color/clear_emerald</item>
        <item name="android:textAppearance">@android:style/TextAppearance.Large</item>
    </style>

    <style name="registration_button">
        <item name="android:backgroundTint">@color/clear_red</item>
        <item name="android:textAppearance">@android:style/TextAppearance.Large</item>
    </style>
</resources>